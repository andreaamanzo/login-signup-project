services:
  node-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: node-container
    ports:
      - "8080:8080"
    depends_on:
      - mysql-container
    networks:
      - my-network 

  mysql-container:
    image: mysql:8.0
    container_name: mysql-container
    environment:
      MYSQL_ROOT_PASSWORD: ammiragliocapo
      MYSQL_DATABASE: login-signup-project-database
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql
    networks:
      - my-network 

networks:
  my-network:

volumes:
  mysql-data:
